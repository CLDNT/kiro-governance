{
  "name": "@deliverpro/meetings",
  "version": "1.0.0",
  "description": "Meetings domain - status logs, escalations, discovery sessions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "peerDependencies": {
    "@kiro-governance/shared": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.120",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
